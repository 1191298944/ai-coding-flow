---
description: 四路并行流程中所有子代理（clarifier / spec-writer / implementor / test-writer）的通用约束。违反任一约束视为本次操作不合格，必须立即停止并回报主 agent。
alwaysApply: false
---

# ai-coding-sub-agent 通用约束

> 被 clarifier / spec-writer / implementor / test-writer 引用。接受主 agent 调用后，在执行任何操作前先读此规则。

---

## 文件访问合规

**执行任何读/写前，对照 ai-coding-file-conventions skill 中自己角色的权限清单。**

- 准备读"禁止读"范围内的文件 → **立即停止，回报主 agent，说明越界文件路径**
- 准备写"禁止写"范围内的文件 → **立即停止，回报主 agent，说明越界文件路径**
- 已执行的越界操作产生的内容 → 视为无效，不得继续使用

**自检**：本次涉及的每个文件路径，能在 ai-coding-file-conventions 对应角色的"允许"列表中找到依据吗？找不到 → 禁止操作，回报主 agent。

---

## 歧义处理

- 遇到需求歧义或输入信息不足 → **禁止**自行选择一种理解继续执行；**必须**回报主 agent，说明歧义点
- 遇到"两种解读都合理"的情况 → 列出两种解读回报主 agent，等待决策

**自检**：本次执行中有没有"不确定但还是选了一种继续"的节点？有 → 停止，回报主 agent。

---

## 完成返回格式

任务完成后，返回主 agent 的内容必须包含：
- **产物标识**：已创建或修改的文件绝对路径；clarifier 以业务名代替（业务名可派生全部路径）
- **待决策项**（如有）：遇到歧义或无法完成的事项及原因

**自检**：返回内容里有没有"产物标识"和"待决策项"两项？缺任一 → 补全再返回。

---

## 生效信号

- 子代理调用历史里没有读取"禁止读"文件的工具调用
- 子代理调用历史里没有写入"禁止写"路径的工具调用
- 遇到歧义时能看到回报主 agent 的文本，而不是直接产出了一个选择结果
- 每次完成都有产物路径列表回传给主 agent
