---
description: 指导 AI 创建 Cursor rule (.cursor/rules/*.mdc) 或 skill (SKILL.md)。当用户要求"创建 rule / 新建规则 / create rule / 创建 skill / 新增技能 / 写个规则 / 写个技能"时应用，保证产出精简、具体、可验证。
alwaysApply: false
---

# Rule / Skill 编写纪律（四条原则 + 一条前置判断）

> 风格借鉴 [andrej-karpathy-skills](https://github.com/forrestchang/andrej-karpathy-skills)：简洁、直接、可验证。
>
> **使命**：产出 AI 能稳定执行、diff 里看得出效果的 rule / skill。

**权衡说明**：本纪律偏向精简优先。一次性个人偏好走对话即可，不必每次都走完整流程。

---

## 0. 先判断：Rule 还是 Skill？

**不要两个一起写。选一个最合适的载体。**

| 诉求 | 载体 |
|---|---|
| 持续约束 AI 行为（风格、禁止项、命名） | Rule |
| 打包完整工作流 / 操作步骤 / 方法论 | Skill |
| 针对特定文件类型生效（`**/*.ts`、`**/*.vue`） | Rule + `globs` |
| 用户显式"启动"的复杂任务（创建 PR、配置环境） | Skill |
| 每次对话都要注入的底线要求 | Rule + `alwaysApply: true` |

**自检一句话**：它是"AI 干活时的约束"？→ Rule。是"AI 要去完成的一项工作"？→ Skill。

---

## 1. 明确用途先于编写

**不要为不存在的问题创建规则。**

动手之前（必要时通过 AskQuestion 明确）：
- 用一句话写出要解决的具体问题（谁、什么时候、怎么错了）
- 列出没这条规则会发生的坏结果
- 搜索已有 rule / skill，有覆盖就合并，**不要新建**

反模式：
- ❌ "先写一个代码审查规范，以防万一" → 不是具体问题
- ✅ "AI 生成的 commit 经常漏掉 `Made-with: Cursor` trailer，要强制" → 具体、可验证

---

## 2. 极简优先

**一个文件一个主题。50 行软上限。**

- 一个 rule 只讲一件事。两件 → 拆成两个。
- 能一句话讲清的不要写段落。
- **禁止**铺陈背景、哲学、愿景。
- 优先表格 / 对比 / 清单，少用长段落。

Skill 同理：一个 SKILL.md 一个工作流；`description` 必须让 AI 一眼知道何时触发。

**自检**：把内容删掉一半，规则还能执行吗？能 → 说明那一半是废话，删掉。

---

## 3. 具体而非抽象

**写 AI 能直接 diff / lint 的指令。不要写"建议"。**

语言纪律：
- ❌ "尽量使用 TypeScript"、"建议加注释"、"考虑性能"、"遵循良好实践"
- ✅ "新文件必须用 `.ts` 后缀"、"public 方法必须有 JSDoc 的 `@param`"

必备要素：
- **至少一组**正反对比（❌ Bad / ✅ Good），优先放代码块
- **明确的触发条件**（什么场景、什么文件、什么关键词）
- **一个最小可验证的示例**

反模式：
- ❌ "应遵循良好的编码习惯" → 无法执行
- ✅ "函数参数 > 3 个时，必须封装为对象参数" → 可 lint、可 diff

---

## 4. 自检与可验证

**给出"规则生效的可观测信号"。否则 AI 不知道何时算完成。**

每个 rule / skill 结尾必须有（二选一或都有）：
- **可观测信号**：如 "diff 里没有未请求的格式化改动"、"commit message 含 `Made-with: Cursor`"
- **最小 checklist**：≤ 5 条，每条能回答 yes/no

Skill 额外要求：
- 多步流程的**每一步**要有"验证点"（如何确认这一步完成了）
- 整体要有明确的"完成标准"

---

## Frontmatter 速查

```yaml
---
description: 一句话说清"做什么 + 何时触发"，让 AI 能自动判断激活
globs: **/*.ts        # 可选，按文件激活
alwaysApply: false    # true 会每次对话都加载，慎用
---
```

激活优先级：`alwaysApply: true` > `globs` 匹配 > `description` 触发词匹配。

**自检**：description 里有没有写清楚"什么场景 / 什么关键词应该用我"？

---

## 落地检查清单

- [ ] 目标问题具体、可复现，不是"以防万一"
- [ ] 载体选对（Rule vs Skill）
- [ ] 单文件 ≤ 50 行（复杂 skill 可放宽，但 description 必须精简）
- [ ] frontmatter 三项（`alwaysApply` / `globs` / `description`）至少一项明确
- [ ] 每条规则都能 diff / lint / 清单 验证
- [ ] 至少一组正反示例
- [ ] 结尾有"生效信号"或 checklist

---

## 这些原则生效的信号

- rule / skill 文件**短小干净**，无冗余
- AI 读完能**直接执行**，不需要二次澄清
- diff 里能**肉眼看出**这条规则的效果
- 团队成员读完 **30 秒**能理解作用与适用范围
