---
description: 新开发任务启动"规格-实现-测试"三路并行生成流程，三方只对用户原始需求负责、彼此产物不互见，用于独立交叉验证 AI 理解偏差。当识别到需要新建 Controller + Service + Mapper 完整链路 / 新接口 / 新模块时，主动向用户提议启动；用户显式说"走三路流程"也可触发。仅新开发生效，修改既有业务不触发。
alwaysApply: false
---

# 规格-实现-测试 三路并行流程

> 与已有规则分工：`karpathy-guidelines` 管「如何写代码」，`ai-driven-development` 管「AI 与人类协作边界」，`mysql` / `git` 管底层安全——本规则只管「新开发任务如何并行编排三个子代理实现独立交叉验证」，不重复上述规则内容。冲突时以专项规则为准。

## 0. 触发判断

**仅在新开发任务启动；修改既有业务不启动。**

- 新开发：新建 Controller + Service + Mapper 完整链路 / 新业务模块 / 从零写一个新接口
- 非新开发：bug fix / 字段微调 / 已有接口加参数 / 重构既有方法
- 识别到新开发信号时，主 agent **必须先问用户**是否启动本流程；用户确认后执行，否则走常规 Agent 模式

**自检**：这次改动是否新增了一个独立业务概念？说不出来 → 不是新开发，停用本规则。

---

## 1. 唯一真相源：用户原始需求（含澄清问答）

**主 agent 只搬运，不转述。**

- 发起子代理时的 prompt，必须以「用户原始消息原文引用」作为需求来源，不得替换成主 agent 的摘要或理解
- 用户修订需求时，主 agent 原封不动把修订消息追加传递给子代理，不得在「过渡消息」里加入自己的理解
- 所有子代理**共享同一份**真相源全文（原始需求 + 澄清问答 + 所有修订）

### 1.1 需求澄清（启动三路前的必需前置）

正式启动 `spec-writer` / `test-writer` 前，主 agent 先把用户原始需求交给 `implementor` 做需求澄清（`implementor` 最早要写代码，最容易发现模糊点；遵循 `karpathy-guidelines` / `ai-driven-development` / `scenario-fit` 的需求澄清纪律）。

澄清通过主 agent 中转：

```
用户原始需求 → implementor（发起澄清问题） → 主 agent → 用户（回答）
                                                         ↓
                                  → 主 agent → implementor 收到答复
```

澄清结束后，**最终真相源 = 原始需求 + 全部澄清问答**。主 agent 把这份完整真相源**同时**发给 `spec-writer` / `implementor` / `test-writer`，保证三方上下文一致。

**自检**：①发给子代理的 prompt 里有没有主 agent 自己的需求解读？②是否包含完整的澄清问答？任一违反 → 补齐或替换成原文引用。

---

## 2. 三路独立启动

**用 `Task` 工具按顺序串行启动三路；信息独立靠 Task 独立上下文 + prompt 禁读保证（时间串行 ≠ 信息共享）。**

| 角色 | 可读 | 禁读 | 产物 |
|---|---|---|---|
| `spec-writer` | 用户需求原文、数据库 schema、业务数据样本 | `src/` 下任何业务代码、其他子代理产物 | 结构化需求文档 `.md` |
| `implementor` | 用户需求原文、`src/` 已有代码、项目技术栈规则 | `spec-writer` 产物、`test-writer` 产物 | Controller / Service / Mapper / XML / VO / DTO |
| `test-writer` | 用户需求原文、`spec-writer` 产物 | `implementor` 产物 | `<专用测试模块>/src/test/java/` 下测试类 |

- 每个子代理对应一个同名 skill（`spec-writer` / `implementor` / `test-writer`），主 agent 在 prompt 中指示子代理读取并遵循
- **子代理模型默认与主 agent 一致**：发起 Task 时**不传 `model` 参数**；除非用户**明确要求**使用其他模型

### 启动顺序与各阶段职责

**启动顺序（主 agent 按阶段串行推进，前一步完成后才启动下一步）**：

1. `implementor` 阶段 1 - **需求澄清**
2. 澄清完毕（最终真相源 = 原始需求 + 澄清问答）
3. `implementor` 阶段 2 - **编程**
4. `implementor` 完成 → 主 agent 启动 `spec-writer`
5. `spec-writer` - **需求文档编写**
6. `spec-writer` 完成 → 主 agent 启动 `test-writer`
7. `test-writer` - **首次测试**
8. `test-writer` 完成 → 主 agent 按 §4 汇报用户 + 进入修订回路

**各阶段输入输出（主 agent 发起子代理时的信息来源与产物落位）**：

| 子代理 | 阶段 | 输入 | 产物 |
|---|---|---|---|
| `implementor` | 1. 澄清 | 用户原始需求 + `src/` 既有代码 | 澄清问题清单（交主 agent 中转给用户） |
| `implementor` | 2. 编程 | 完整真相源 | 业务代码（Controller / Service / Mapper / XML / VO / DTO） |
| `implementor` | 3. 修订 | 主 agent 的测试失败**自然语言总结** + 自己既有代码 | 修改后代码 / 或"建议仲裁 b/c"的判断 |
| `spec-writer` | 编写 | 完整真相源 + 数据库 schema | `<类名>Test.md` 结构化需求文档 |
| `spec-writer` | 修订 | 用户修订原文 + 自己既有文档 | 更新后需求文档 |
| `test-writer` | 首次 | 完整真相源 + `spec-writer` 产物 + DTO/VO 接口契约 | 测试类 `*Test.java` + `mvn test` 结果 |
| `test-writer` | 修订 | 最新 `spec-writer` 产物 + 自己既有测试 | 更新后测试类 + 新测试结果 |

主 agent 自身职责：**流程总控 + 信息搬运 + 用户汇报**。禁止自己解读或添加 prompt 以外的业务理解。

### 子代理 Prompt 必含要素

主 agent 发起子代理时，prompt **必须**包含以下要素（字句与详略由主 agent 按场景定制，缺一即违反本规则）：

| # | 要素 | 最低内容 |
|---|---|---|
| 1 | 流程与规则手册指向 | `rule` 文件路径 + 对应 `skill` 文件路径（必读） |
| 2 | 唯一真相源路径 | 用户需求原文 / 需求文档 / 依据文件的具体绝对路径 |
| 3 | 角色边界 | 允许读清单 + 严格禁读清单，具体化到本次场景涉及的文件 |
| 4 | 强制项 | 对应 skill 的技术栈 / 代码规范强制约束（如测试栈 / 编码规范） |
| 5 | 产出位置 | 按 `rule §3` 规范 + 本次场景的具体文件路径 |
| 6 | 脚手架 / 前置状态 | 本次场景相关的环境就绪情况（已搭 / 未搭 / 缺什么） |
| 7 | 冲突或失败时的行为 | 按对应 skill 的失败处理章节，不自行下结论 |
| 8 | 汇报格式 | 明确要求子代理返回什么信息（产出路径 / 用例数 / 执行结果等） |
| 9 | 绝对约束 | 禁止改哪些文件、禁止产出什么（越界视为任务失败） |

**自检**：发起子代理的 prompt 是否满足两件事——①九项要素齐全；②不含其他子代理的产物？任一项 no → 作废重组 prompt。

---

## 3. 产物落位规范

**需求文档与测试类同目录同名，集中放在项目的专用测试模块下，强调「需求是测试的真相源」。**

- `implementor` 产物：按项目既有模块规范落位（如 `scm-rgl/src/main/java/...`）
- `test-writer` 产物：`<专用测试模块>/src/test/java/<包路径>/<类名>Test.java`（项目的专用测试模块，如 `scm-test`）
- `spec-writer` 产物：`<专用测试模块>/src/test/java/<包路径>/<类名>Test.md`（与测试类**同目录同名**，仅后缀不同）

**自检**：需求文档和对应测试类是否同目录同名，且放在项目专用测试模块下？不是 → 按 test-writer 的类名和路径移动。

---

## 4. 修订回路

**核心：每次测试跑完，主 agent 必须第一时间汇报用户。用户决定下一步。**

### 4.1 汇报必需

无论测试全 PASS 还是有 FAIL，`test-writer` 完成后，主 agent **第一动作**是把结果原样汇报给用户（用例名 / 预期 vs 实际 / 对应需求章节）。**禁止**在用户看到前自行启动修订——用户可能基于结果调整需求。

### 4.2 反馈通道（污染 vs 反馈的边界）

用户选择"假设代码偏差（a）"时，主 agent 把测试失败**用自然语言总结**发给 `implementor`；`implementor` 据此分析 + 修改，**不读** `test-writer` 产物（保持独立性）。

| 允许反馈（主 agent 转述） | 禁止反馈（污染） |
|---|---|
| 用例名、预期行为（**引用需求章节**）、实际返回的差异描述 | 直接贴测试代码文件 |
| 数据库中观察到的状态描述 | 直接贴断言代码 / 具体断言数值 |
| 需求文档里涉及的章节引用 | 其他子代理产物原文 |

### 4.3 循环流程

```
 test-writer 跑测试
        ↓
 主 agent 汇报用户【必需，不可跳】
        ↓
   用户决定
  ┌──────┼─────────────┬──────────────┐
 全 PASS  FAIL/a 代码偏   FAIL/b 规格偏   FAIL/c 测试偏
  ↓          ↓             ↓                ↓
 完成   主 agent 总结→  spec-writer    test-writer
          implementor    从头重写         从头重写
            ↓
          修改代码
            ↓
        主 agent → test-writer 重跑
            ↓
         回到"汇报用户"
```

循环直到用户判定完成（全 PASS / 用户主动终止 / 需求调整后重启流程）。

### 4.4 用户修订需求

用户调整需求时，主 agent 把修订**原文**追加到真相源（§1 完整真相源的一部分），同时发给 `spec-writer` / `implementor` 并行增量更新；两方完成后 `test-writer` 基于最新 spec 产物重跑，回到 §4.1 汇报用户。

**自检**：①测试跑完第一件事是不是汇报用户？②反馈通道有没有贴任何子代理产物原文？任一违反 → 作废重组。

---

## 落地检查清单

- [ ] 新任务启动前已问用户是否走本流程
- [ ] 子代理 prompt 里引用的是用户原文，没有主 agent 的解读
- [ ] 三路子代理的 prompt 无交叉污染（没有互相贴产物）
- [ ] 产物落位符合「需求文档与测试类同目录同名」规范
- [ ] 测试失败时交用户仲裁，不自行判断

## 生效信号

- 新功能启动前有一次「是否走本流程」的用户确认
- 子代理 prompt 中能肉眼看到用户消息原文引用
- `<专用测试模块>/src/test/java/` 下同一包路径出现同名的 `.java` 和 `.md` 成对文件
- 测试失败时产出结构化的「规格说 A / 代码做 B / 测试断言 C」三方差异报告
