---
description: LLM 设计适度性（三条核心原则）：先问会怎么用、复杂度匹配使用规模、选择不匹配时说出来。防止 LLM 两端出错——小需求过度设计，大场景朴素实现。
alwaysApply: true
---

# 场景驱动设计（三条核心原则）

> 定位：`karpathy-guidelines` 处理**做什么 + 怎么做**（意图澄清 + 执行纪律），本规则处理**做到什么量级**（使用规模 + 设计匹配）。两者冲突时本规则优先——"简洁"与"复杂"都是使用场景的函数。

---

## 1. 先问会被怎么用

**设计之前，先搞清楚：会处理多少、被调用多频繁、多少人在用、会不会复用到别处、会不会变。**

- **不要**假设"一次性、小规模、单人使用"——这是 LLM 最常犯的错。
- **不要**假设"大规模、高并发、通用可扩展"——这是过度设计的起点。
- 用户没说就**问**，不要拿猜测当事实。
- 明显的小任务（typo、一行修复、纯展示调整）直接做，不必追问规模。

**自检**：被问到"这代码会处理多少 / 多频繁 / 谁在用"，答得上来吗？答不上 → 回去问。

---

## 2. 复杂度匹配使用规模

**代码复杂度必须匹配它将会怎么被使用。过复杂 = 浪费，过简单 = 埋雷。**

- **不要**为只会调用一次的代码写框架、插件、配置系统。
- **不要**为少数固定情况写"可切换"的动态实现。
- **不要**在明知数据量大时逐个处理、一次加载、重复计算。
- **不要**在明知高频访问时每次从头算、每次全量返回。
- **当一个抽象只有一处使用时，重写成直接调用。**

**自检**：使用规模提升 10 倍时，哪一行会先出问题？答不上 → 规模假设错了，重估再设计。

---

## 3. 选择不匹配时，说出来

**主动选了和使用规模不匹配的方案时，必须写进改动说明，不要静默执行。**

- **为省事先写简化版**：标出后续需要升级的触发条件。
- **为未来先写复杂版**：说清是为哪个**已知**的未来需求，禁止"以防万一"。
- **不要**让用户在出事之后才发现你偷工。
- **不要**让用户在 review 时才发现你超纲。

**自检**：改动说明里有没有写清"我对规模做了什么假设 / 主动妥协了什么"？没有 → 补上。

---

## 这些原则生效的信号

- 代码里能看出"为什么这么设计"的痕迹，而不是一刀切的默认模板。
- 改动说明**回答过**或**问过**：会处理多少、多频繁、谁在用。
- 没有为一处使用准备的多层抽象。
- 没有在已知大规模下写出的朴素实现。
- 主动做取舍时，理由在改动说明里，不用靠代码考古来发现。
