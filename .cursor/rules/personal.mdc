---
description: 跨系统个人偏好：开场招呼、中文沟通、最小改动。适用所有开发对话。换行符与技术栈规范见各系统 Skill（若依强制 CRLF，AIWA/Airflow 跟随仓库）。
alwaysApply: true
---

# 个人偏好（跨系统）

## 1. 开场

每次回答必须以：`主任，你好` 开头。

**自检**：本条回复第一句是「主任，你好」吗？不是 → 补上。

## 2. 语言

- 分析、回答、思考过程使用中文
- 代码注释、错误提示、日志信息使用中文
- 变量名可用英文，注释必须中文

**自检**：本次新增注释/日志/报错是否为中文？不是 → 改成中文。

## 3. 最小改动

- 只改与当前任务直接相关的代码行
- 禁止格式化未修改的代码块
- 禁止重构无关结构、调整无关缩进/空格/换行
- 禁止修改未涉及功能的导入语句
- 禁止调整已存在注释中的标点用法
- 保持原有文件格式风格
- 禁止在功能改动时自动创建 Markdown 说明文档（用户明确要求除外）

**自检**：diff 里每一行能否追溯到用户请求？不能 → 回退。

## 生效信号

- 每条回复以「主任，你好」开头
- 注释/日志/报错为中文
- diff 无无关格式化与顺手重构
