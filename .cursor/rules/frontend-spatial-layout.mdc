---
description: 前端布局调整：用户用空间方位词（上面/下面/同一行/挤到/位置）描述视觉位置，不是代码顺序。优先截图获取视觉真值；截图不可用时先推演渲染结果再改代码。
alwaysApply: false
---

# 前端布局：空间描述 ≠ 代码顺序

> 定位：专管"用户说位置 → AI 理解位置"，不涉及代码风格与业务逻辑。与 karpathy-guidelines 分工：karpathy 管如何写代码，本规则管如何理解用户的空间描述。

用户用方位词描述的是**屏幕上的视觉位置**，不是代码中的行号或数组下标。

## 1. 优先截图，截图不可用再推演

截图是视觉真值，推演是近似值。

- 有截图工具可用（如浏览器 MCP、browser-use）→ **先截图**，直接读取当前视觉布局，再决定改法。
- 截图不可用 → 读布局代码，推演当前可见元素的渲染位置，画出改前/改后示意。

禁止跳过这两步，直接把方位词映射为代码位置。

**自检**：能确认改前的视觉布局吗（截图或推演）？不能 → 先确认，再动代码。

## 2. 空间词 = 视觉位置，不是代码顺序

| 用户说 | 含义 |
|---|---|
| 在 X 上面 | 渲染在 X 所在行的上方 |
| 和 X 同一行 | 与 X 在同一视觉行内并排 |
| 挤到 / 被挤到 X 同行 | 布局变化后，与 X 落在同一视觉行 |
| 在 X 的位置 | 视觉上占据 X 原来所在的位置 |

代码中两个元素相邻，不代表视觉上相邻；代码顺序靠前，不代表视觉上在上方。

**自检**：每个方位词都能对应到具体视觉位置吗？不能 → 向用户确认，禁止猜一个代码顺序直接提交。

## 3. 改动说明包含视觉示意

输出改前/改后视觉示意，让用户能快速验证：

```
改后布局：
行1：A | B
行2：C（整行）
行3：D | E
```

**自检**：改动说明里有"改后布局"描述吗？没有 → 补一句。

---

## 生效信号

- 截图可用时先截图再改代码，不用推演
- 截图不可用时有"改前推演"步骤
- 改动说明包含改后视觉描述
- 不出现"在 X 上面"但实际视觉在下面的情况
